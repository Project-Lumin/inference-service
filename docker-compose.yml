version: '3.8'
services:
  inference_service:
    build: .
    container_name: inference_service
    ports:
      - "8888:8888"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
